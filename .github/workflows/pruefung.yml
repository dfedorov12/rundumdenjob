name: Prüfung

# Läuft bei jedem Push und Pull Request. Verhindert, dass ein Syntaxfehler
# oder eine kaputte Kernlogik auf GitHub Pages landet – die Seite wird ohne
# Build-Schritt ausgeliefert, es gibt also keine zweite Instanz, die das
# abfängt.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  pruefung:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Syntax aller JavaScript-Dateien
        run: |
          fehler=0
          for f in js/*.js tests/*.mjs; do
            if node --check "$f"; then
              echo "  ok   $f"
            else
              echo "  FEHLER $f"; fehler=1
            fi
          done
          exit $fehler

      - name: Spaltennamen-Toleranz (graph.js)
        run: node tests/test-graph-fieldmap.mjs

      - name: Import/Export-Kernlogik (impexp.js)
        run: node tests/test-impexp.mjs

      - name: Konfiguration und Doku auf Konsistenz prüfen
        run: node tests/test-konsistenz.mjs
